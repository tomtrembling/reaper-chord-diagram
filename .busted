return {
  default = {
    ROOT = { "spec" },
    -- `spec/?.lua` is on the path so shared test data (spec/fixtures/*) can be
    -- required by name. Only files matching `pattern` are run as tests.
    lpath = "src/?.lua;src/?/init.lua;spec/?.lua",
    pattern = "_spec",
  },
}
